general:
  branches:
    only:
      - master

machine:
  php:
    version: 7.1.0RC2
  node:
    version: 4.2.2
  ruby:
    version: 2.2.2
  timezone:
    Europe/Amsterdam

dependencies:
  override:
    - composer install --no-interaction

test:
  override:
    - ./vendor/bin/phing
