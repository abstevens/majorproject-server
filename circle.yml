general:
  branches:
    only:
      - master

machine:
  php:
    version: 7.0.11
  node:
    version: 4.2.2
  ruby:
    version: 2.2.2
  timezone:
    Europe/Amsterdam

dependencies:
  override:
    - composer install --no-interaction

test:
  override:
    - ./vendor/bin/phing
