general:
  branches:
    ignore:
      - master

machine:
  php:
    version: 7.0.7
  node:
    version: 5.10.1
  ruby:
    version: 2.3.1
  timezone:
    Europe/Amsterdam

dependencies:
  override:
    - composer install -d ./application --no-interaction

test:
  override:
    - ./application/vendor/bin/phing
