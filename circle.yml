general:
  branches:
    ignore:
      - master

machine:
  php:
    version: 5.5.21
  node:
    version: 4.2.2
  ruby:
    version: 2.2.2
  timezone:
    Europe/Amsterdam

dependencies:
  override:
    - composer install -d ./application --no-interaction

test:
  override:
    - ./application/vendor/bin/phing
